# プライバシーガイドライン & 個人情報保護運用規程

---

## 1. はじめに

親亡き後支援データベース（oya-inai-db）は、知的障害・精神障害のある方の支援情報を管理するシステムです。扱うデータには**要配慮個人情報**（障害の種類・等級、医療情報、生育歴等）が多く含まれるため、個人情報保護法および関連ガイドラインに準拠した運用が必要です。

本ガイドラインは、本システムを導入する事業所が、個人情報保護規程を整備する際の参考資料として作成されたものです。各事業所の既存規程と照らし合わせ、必要に応じてカスタマイズしてください。

---

## 2. データの保管場所と流れ

### 2.1 データの保管

本システムのデータ本体（Neo4j グラフDB）は**ローカル環境**（利用者のパソコン内の Docker コンテナ）に保管されます。操作画面（Web画面）と API サーバーも同じパソコンの中で動きます。

```
[パソコン内]
├── Web画面 (Next.js, port 3001)   ← ブラウザで開く操作画面
├── API サーバー (FastAPI, port 8001)
├── Neo4j コンテナ (port 7687 / 7474) ← 障害福祉データ
└── neo4j_data/ ← 永続化ボリューム
```

> **重要（外部送信があるのは「Claude への依頼」だけ）**: Web 画面の機能（台帳の閲覧、緊急照会、更新期限アラート、出来事の記録、面談記録、エコマップ、知識グラフ）は、**外部に一切データを送らずローカルだけで完結**します。一方、新しい方の登録・まとめ入力・照会を **Claude** に頼んだ場合は、依頼した内容が Anthropic 社のサーバーへ送信されます。詳細は 2.2 と 2.3 を必ず読んでください。

### 2.2 外部AIとデータの扱い（重要）

本システムで使う AI は **Claude**（Anthropic 社）一本です（2026-08 方針決定）。以前あった AI の 3 択（Ollama / Gemini / Claude API）と、アプリ内の AI 機能（意味検索・AI チャット・ナラティブ抽出・音声の文字起こし）は廃止しました。Claude は Claude Desktop から MCP という仕組みでデータベースに直接つながります（→ ç¦ç¥å°éè·ã®ããã®å®å¨å°å¥ããã¥ã¢ã«.md 第 6 章）。

データの行き先は、操作によって次の 2 通りに分かれます。

| 操作 | データの行き先 |
|---|---|
| Web 画面での閲覧・記録（台帳・緊急照会・期限アラート・出来事の記録・面談記録・エコマップ・知識グラフ） | **外に出ない**（パソコン内で完結） |
| Claude への依頼（新しい方の登録・まとめ入力・照会・書類の下書き） | **Anthropic 社のサーバー**へ、依頼文とそこに含まれる支援情報が送信される |

**必ず守ること:**

- **導入前に、契約する Claude プランのデータ取り扱い方針（入力内容がモデルの学習に利用されるかどうか等）を確認し、組織の個人情報保護規程と照らし合わせて判断してください。** ポリシーは変更されうるため、年 1 回程度の再確認を推奨します（→ 付録）。
- **Claude の出力は、登録・判断の前に必ず人間（支援者）が確認してください。** AI が原文にない情報を補って生成することがあるため、原文と突き合わせ、不明な項目は不明のまま残してください。登録結果は Web 画面のクライアント詳細で目視確認します。
- **緊急時の禁忌事項・連絡先の確認には、Claude ではなく Web 画面の緊急照会（クライアント詳細）を使ってください。** AI を経由しないため、速く、内容が変わる余地がありません。
- 研修・説明会で Claude の操作を見せる場合は、実データではなく**デモデータ**を使ってください（→ 6.1）。

**Claude を使わない運用について**
閲覧と日々の記録（台帳・緊急照会・期限アラート・出来事の記録・面談記録）は Claude なしで動作し、その間データは 1 バイトも外部に出ません。ただし、**新しい方の登録とまとめ入力は Claude が担う設計**のため、データベースを育てていくには Claude の利用（＝Anthropic への送信を伴う運用）が実質的に必要です。この前提を踏まえて導入判断をしてください。

### 2.3 Claude（Anthropic）への送信範囲

Claude に依頼したとき、**Anthropic 社のサーバー**へ送信されるのは次のものです。Claude を設定・利用しなければ、何も送信されません。

| 内容 | 送信契機 |
|---|---|
| 依頼文そのもの（そこに含まれる氏名・障害・禁忌等の支援情報を含む） | Claude に入力・貼り付けしたとき |
| Claude がデータベースから読み取った内容（照会結果） | Claude が MCP 経由で Neo4j を検索したとき |
| Claude に読ませたファイルの中身（面談メモ・手帳の写し等） | filesystem MCP でファイルを渡したとき |

- 旧バージョンにあった「登録時に自動で外部 AI へテキストを送る」処理（意味検索用のベクトル生成）は、コードごと廃止しました。**Web 画面での登録・閲覧では、いかなる自動送信も発生しません。**
- 送信されるのは依頼のたびにその依頼の範囲だけです。データベース全体が同期・複製されることはありません。

### 2.4 外部への通知連携（現時点では未搭載）

**現在の Web画面スタックには、LINE 等の外部サービスへ通知を送る機能は含まれていません。** 緊急照会はすべてローカルの Neo4j 内で完結します。

将来、外部メッセージングサービス（LINE Messaging API 等）と連携する場合は、送信内容がその事業者のサーバーを経由することになるため、以下を必ず守ってください。

**対策:**
- 通知に含める情報は必要最小限（識別子と緊急レベル程度）にとどめ、実名や病名を載せない
- 通知先グループのメンバー管理を適切に行い、退職者の即時除外を徹底する

---

## 3. 仮名化機能の運用

### 3.1 仮名化とは

仮名化（Pseudonymization）とは、個人を直接特定できる情報（氏名、生年月日、電話番号等）を、識別記号や部分マスクに置き換える処理です。本システムでは、**表示時のみ**仮名化を行い、データベース内の実データは変更しません。

### 3.2 仮名化モード

| モード | 説明 | 用途 |
|--------|------|------|
| `off` | 仮名化なし | 通常の業務運用 |
| `mask` | 部分マスク（山田→山●●●） | 研修、説明会、デモ |
| `pseudonym` | 架空名に置換（山田→青山あおい） | テスト、開発、公開資料 |

### 3.3 仮名化の対象フィールド

| フィールド種別 | mask モード例 | pseudonym モード例 |
|--------------|-------------|-------------------|
| 人名 | 山●●● | 青山あおい |
| 電話番号 | 090-\*\*\*\*-5678 | 090-0000-0001 |
| 生年月日 | 1995-XX-XX | 1995-07-22 |
| 住所 | 東京都●●区●●● | ○○県桜ヶ丘1-1-1 |
| 医療機関名 | ●●病院 | さくら病院 |
| 組織名 | ●●事業所 | あさひ福祉協議会 |

### 3.4 仮名化の有効化手順

```bash
# 1. .env に設定
PSEUDONYMIZATION_ENABLED=true
PSEUDONYMIZATION_MODE=mask          # または pseudonym
PSEUDONYMIZATION_SEED=any-string    # セッション固定シード

# 2. サービス再起動
docker compose restart
```

> **適用範囲**: 仮名化は Python 側の処理経路（`lib/pseudonymizer.py` を通る出力）で有効になります。Web画面から見える表示すべてに自動適用されるわけではないため、研修・説明会では**デモデータの使用（6.1）を第一選択**にしてください。

### 3.5 仮名化の安全上の例外

以下の情報は、**安全に直結するため仮名化の対象外**です:

- **NgAction（禁忌事項）**: リスクレベル、行為内容、理由
- **CarePreference（推奨ケア）**: ケアの指示内容
- **Certificate（手帳情報）**: 種類、等級（氏名は仮名化対象）

これは、緊急時に正確な禁忌情報やケア情報にアクセスできることが、利用者の安全を守るために不可欠だからです。

---

## 4. アクセス管理

### 4.1 物理的アクセス管理

本システムはローカル環境で動作するため、パソコンへの物理的アクセス管理が重要です。

- パソコンのログインパスワードを適切に設定する
- 離席時は画面ロックを徹底する
- パソコンを共有する場合は、OS のユーザーアカウントを分離する

### 4.2 Neo4j データベースへのアクセス

デフォルトのパスワード（`password`）は本番運用前に必ず変更してください。

```bash
# docker-compose.yml の環境変数を変更
NEO4J_AUTH=neo4j/your_strong_password
```

### 4.3 ネットワークアクセス

Neo4j はデフォルトで `localhost` のみリッスンします。同一ネットワーク内の他のパソコンからアクセスする場合は、ファイアウォール設定とアクセス制御を適切に行ってください。

---

## 5. バックアップと廃棄

### 5.1 バックアップ

```bash
# 定期バックアップ（推奨: 週次）
docker compose stop
cp -r neo4j_data neo4j_data_backup_$(date +%Y%m%d)
docker compose start
```

バックアップデータにも個人情報が含まれます。暗号化したUSBや施錠できる場所に保管してください。

### 5.2 データの廃棄

利用者が退所・転出した場合や、保存期間を超過した場合のデータ廃棄:

```cypher
// 特定クライアントのデータを完全削除
// Neo4j Browser（http://localhost:7474 / neo4j・password）で実行する
// ※ この操作は取り消せません。実行前に必ずバックアップを取ること
MATCH (c:Client {name: "氏名"})-[r]->(n)
DETACH DELETE c, n
```

廃棄の記録は監査ログ（AuditLog）に自動記録されます。

---

## 6. 研修・説明会でのデモ

### 6.1 デモデータの使用（推奨）

研修や説明会では、実際の個人情報を使わず、デモデータを使用してください。

```bash
# デモデータの投入
./installer/load-demo-data.sh

# デモ終了後にデモデータを削除
./installer/load-demo-data.sh --remove
```

デモデータは `isDemo = true` フラグが付与されており、実データと区別できます。

### 6.2 仮名化モードでの実データ使用

デモデータでは不十分な場合（実際の業務フローを見せたい場合）は、仮名化モードを有効にして実データを使用できます。

```
# .env を一時的に変更
PSEUDONYMIZATION_ENABLED=true
PSEUDONYMIZATION_MODE=mask
```

研修終了後は `PSEUDONYMIZATION_ENABLED=false` に戻してください。

---

## 7. インシデント対応

### 7.1 個人情報漏えい時の対応

1. **即座にアクセスを遮断**: Docker コンテナを停止 (`docker compose stop`)
2. **影響範囲の確認**: 監査ログ（AuditLog）で操作履歴を確認
3. **関係者への報告**: 管理者、個人情報保護責任者に報告
4. **原因の特定と再発防止**: パスワード変更、アクセス権限の見直し
5. **本人への通知**: 個人情報保護法に基づき、本人に通知

### 7.2 監査ログの活用

本システムは全ての操作を AuditLog ノードに記録しています。直近の操作履歴は Web画面のダッシュボードに表示されます。特定の利用者に絞って追う場合は、Neo4j Browser（http://localhost:7474）で次のように確認してください。

```cypher
// 特定クライアントに関する操作履歴
MATCH (a:AuditLog)-[:AUDIT_FOR]->(c:Client {name: "氏名"})
RETURN a.createdAt, a.action, a.details
ORDER BY a.createdAt DESC
```

---

## 8. 法的根拠

### 8.1 関連法令

- **個人情報の保護に関する法律**（個人情報保護法）
- **障害者の日常生活及び社会生活を総合的に支援するための法律**（障害者総合支援法）
- **社会福祉法**

### 8.2 要配慮個人情報

本システムが扱う以下の情報は「要配慮個人情報」に該当します:

- 障害の種類・等級
- 医療情報（かかりつけ医、処方薬等）
- 精神疾患に関する情報
- 生育歴・生活歴

要配慮個人情報の取得には、原則として**本人の同意**が必要です（個人情報保護法第20条第2項）。ただし、障害福祉サービスの提供に必要な場合は、法令に基づく例外が適用される場合があります。

### 8.3 第三者提供

本システムのデータを他の事業所や機関に提供する場合は、原則として本人の同意が必要です。ただし、以下の場合は例外:

- 法令に基づく場合（虐待通報等）
- 人の生命・身体の保護に必要な場合
- 公衆衛生の向上に必要な場合

---

## 9. チェックリスト

導入時に確認すべき項目:

- [ ] 組織の個人情報保護規程に本システムの利用を追記した
- [ ] Neo4j のパスワードをデフォルトから変更した
- [ ] バックアップの手順と頻度を決めた
- [ ] バックアップデータの保管場所を決めた
- [ ] アクセス権限者を明確にした
- [ ] 退職者のアクセス権限削除手順を定めた
- [ ] 契約する Claude プランの**データ取り扱い方針を確認**し、組織の規程と照らして問題ないと判断した
- [ ] Claude の回答・登録結果は**人間が確認してから利用する**運用を周知した
- [ ] 緊急時の禁忌事項確認は Claude ではなく**Web 画面の緊急照会**を使う運用を周知した
- [ ] 研修・デモ時はデモデータまたは仮名化モードを使う方針を周知した
- [ ] インシデント発生時の連絡体制を整備した
- [ ] 利用者・家族への個人情報利用に関する説明と同意取得を行った

---

## 付録: Anthropic（Claude）のデータ取り扱い（確認先）

Claude の利用を始める前に、契約するプランの最新の規約を必ず確認してください。ポリシーは変更されうるため、年 1 回程度の再確認を推奨します。

| 提供元 | 確認先 | 確認する観点 |
|---|---|---|
| Anthropic（Claude） | https://www.anthropic.com/privacy | 入力内容がモデルの学習に利用されるか／保持期間／契約プラン（個人向け・法人向け）による違い |

判断に迷う場合は、まず **Claude を使わない**（Web 画面の閲覧・記録だけの）運用から始めてください。その状態ではデータは外部に出ません。
